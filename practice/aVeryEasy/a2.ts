export function remainder(a: number, b: number): number {
	// TODO: fix
	// TODO: specify return types for every function from now on
	// const division = b / a;
	// console.log(`division: ${JSON.stringify(b)}/${JSON.stringify(a)} = ${JSON.stringify(division)}`);
	return (a % b)
}